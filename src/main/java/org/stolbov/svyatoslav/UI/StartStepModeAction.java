/*
package org.stolbov.svyatoslav.UI;

import org.stolbov.svyatoslav.System.GeneralSystem;

import javax.swing.*;
import java.awt.event.ActionEvent;

public class StartStepModeAction extends AbstractAction {

    private JFrame modeFrame;
    private JFrame stepModeFrame;
    private GeneralSystem generalSystem;

    public StartStepModeAction(JFrame modeFrame, GeneralSystem generalSystem) {
        this.modeFrame = modeFrame;
        this.generalSystem = generalSystem;
    }

    @Override
    public void actionPerformed(ActionEvent e) {
        modeFrame.setVisible(false);
        stepModeFrame = new StepModeFrame(generalSystem);
        stepModeFrame.setSize(1000, 300);
    }
}
*/
